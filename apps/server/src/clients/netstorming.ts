const NETSTORMING_API_URL = "https://api.netstorming.com";

export const fetchFromNetstorming = async (
  endpoint: string,
  payload: object
) => {
  console.log("fetching nestorming...");
  return;
};
